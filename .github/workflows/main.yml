name: Deploy to Cloud Run

on:
  workflow_dispatch:   # Manual trigger
  push:
    branches: ["main" , "de"] # Optional: deploy on push to main

env:
  PROJECT_ID: todoolist-2025
  REGION: us-central1
  REPO_NAME: todo-repo
  SERVICE_NAME: todo-api

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # -------------------------
    # 1. Checkout code
    # -------------------------
    - name: Checkout code
      uses: actions/checkout@v3

    # -------------------------
    # 2. Authenticate to GCP
    # -------------------------
    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}   # <--- Add your GCP JSON key in GitHub secrets

    - name: Set up gcloud
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ env.PROJECT_ID }}

    # -------------------------
    # 3. Authenticate Docker
    # -------------------------
    - name: Configure Docker
      run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

    # -------------------------
    # 4. Build & Push image
    # -------------------------
    - name: Build and Push Docker image
      working-directory: TodoListFirebaseApp   # 👈 where Dockerfile is
      run: |
        IMAGE="us-central1-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}:${{ github.sha }}"
        echo "IMAGE=$IMAGE" >> $GITHUB_ENV
        docker build -t $IMAGE .
        docker push $IMAGE


    # -------------------------
    # 5. Deploy to Cloud Run
    # -------------------------
    - name: Deploy to Cloud Run
      run: |
        gcloud run deploy todo-api \
          --image us-central1-docker.pkg.dev/todoolist-2025/todo-repo/todo-api \
          --region us-central1 \
          --platform managed \
          --allow-unauthenticated \
          --set-env-vars=FIREBASE_SERVICE_ACCOUNT_KEY="${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}"

